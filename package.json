{
  "name": "collaborations-latexdo-org",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p cloudflare/tsconfig.json --noEmit",
    "dev": "npm --prefix cloudflare run dev",
    "deploy": "npx wrangler deploy",
    "typecheck": "tsc -p cloudflare/tsconfig.json --noEmit"
  },
  "dependencies": {
    "lib0": "^0.2.117",
    "y-protocols": "^1.0.7",
    "yjs": "^13.6.31"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260712.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.0"
  }
}
